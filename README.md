# 🎓 مقرري+ (Muqarari+) - التوثيق التقني الشامل

**"سد الفجوة بين النظرية الأكاديمية والتطبيق العملي"**
مشروع برمجي متقدم تم بناؤه للمشاركة في الهاكاثون التقني. تهدف المنصة إلى ربط طلاب هندسة الحاسب والبرمجيات بخبراء الصناعة لإثراء المقررات الأكاديمية بتطبيقات عملية من سوق العمل الحقيقي.

---

## 🛠️ التقنيات المستخدمة (Tech Stack)
* **اللغة الأساسية:** Java 17
* **إطار العمل (Backend):** Spring Boot 3.2.4
* **نظام الحماية:** Spring Security 6 & BCrypt Password Encoder
* **قواعد البيانات:** MySQL 8.x & Spring Data JPA (Hibernate)
* **الواجهة الأمامية (Frontend):** Thymeleaf, HTML5, CSS3, Bootstrap 5.3
* **إدارة الحزم:** Maven
* **نظام التتبع:** Git & GitHub

---

## ⚙️ متطلبات التشغيل (Prerequisites)
يجب على أي مطور في الفريق تثبيت الأدوات التالية قبل البدء:
1. **JDK 17:** (مثل Eclipse Adoptium أو Oracle JDK) وتأكد من ضبط مسار `JAVA_HOME` في الويندوز.
2. **IntelliJ IDEA:** النسخة المجانية (Community) أو Ultimate.
3. **XAMPP:** لتشغيل خادم قاعدة بيانات MySQL محلياً.
4. **Git:** لسحب المشروع وإدارة النسخ.

---

## 🚀 دليل التثبيت والتشغيل خطوة بخطوة
1. **تجهيز قاعدة البيانات:**
    * قم بتشغيل لوحة تحكم XAMPP واضغط `Start` بجوار MySQL.
    * افتح المتصفح على `http://localhost/phpmyadmin`.
    * قم بإنشاء قاعدة بيانات فارغة تماماً باسم: `muqarari_plus` (الترميز: `utf8mb4_general_ci`).

2. **سحب المشروع:**
    * افتح الـ Terminal واكتب: `git clone [رابط-مستودع-جيت-هاب-هنا]`

3. **فتح المشروع وتحميل المكتبات:**
    * افتح المجلد بواسطة IntelliJ IDEA.
    * انتظر حتى يقوم Maven بتحميل جميع المكتبات (Dependencies) من الإنترنت. إذا واجهت خطأ، تأكد من ملف `pom.xml` واضغط على أيقونة `Reload All Maven Projects`.

4. **التشغيل:**
    * اذهب إلى المسار: `src > main > java > com > muqarariplus > platform > MoqarariPlusApplication.java`.
    * اضغط على سهم التشغيل الأخضر (Run).
    * سيقوم النظام (عبر `DatabaseSeeder`) ببناء الجداول آلياً وزرع المقررات والمستخدمين الافتراضيين.

---

## 🔐 الهيكلة الأمنية المعمارية (4-Tier Architecture)
تم بناء نظام حماية صارم يفصل بين 4 أنواع من المستخدمين، ولكل منهم طريقة دخول وصلاحيات محددة:

1. **المالك (SUPER_ADMIN):**
    * **طريقة الدخول:** باستخدام اسم المستخدم `admin` فقط (يُمنع الإيميل).
    * **كلمة المرور الافتراضية:** `123456_Xy`
    * **الصلاحيات:** رؤية إحصائيات النظام الحية، والقدرة الحصرية على (إنشاء / حذف) حسابات الأدمنز.

2. **المشرف (ADMIN):**
    * **طريقة الدخول:** باستخدام اسم مستخدم عشوائي بصيغة `adminXXXX` (يُمنع الإيميل).
    * **بيانات الدخول:** يتم إنشاؤها عبر السوبر أدمن، أو عبر الـ `DatabaseSeeder` أثناء التشغيل الأول (ابحث عن بيانات "سلطان محمد" داخل شاشة Console في IntelliJ).
    * **الصلاحيات:** إدارة المنصة، وتغيير حالة الخبراء من `PENDING` إلى `APPROVED`.

3. **الخبير (EXPERT):**
    * **طريقة الدخول:** بالإيميل فقط.
    * **التسجيل:** يسجل حسابه بنفسه، ولكن يتم تقييده بحالة `PENDING` (ممنوع من الدخول للمنصة) حتى يوافق عليه أحد الأدمنز.
    * **حساب الاختبار:** `expert@muqarariplus.com` | الباسوورد: `expert123` (مفعل آلياً للاختبار).

4. **الطالب (STUDENT):**
    * **طريقة الدخول:** بالإيميل فقط.
    * **التسجيل:** حر، ويدخل للمنصة فوراً.
    * **حساب الاختبار:** `student@muqarariplus.com` | الباسوورد: `student123`

---

## 📜 سجل التعديلات ومراحل تقدم المشروع (Changelog & Journey)
منذ بداية العمل على المشروع، قمنا بإجراء التعديلات الجذرية التالية للوصول للنسخة الحالية المستقرة:

### 1. إصلاحات البنية التحتية والـ Maven
* **تنظيف `pom.xml`:** تم حل مشكلة `ModelParseException` الناتجة عن كتابة أوامر الـ Terminal داخل ملف الـ XML بالخطأ.
* **تحرير المنافذ (Ports):** حل مشكلة `Failed to delete target` بإيقاف الخادم الذي كان يضع قفلاً (Lock) على ملفات المشروع أثناء عملية التنظيف (`mvn clean`).

### 2. هندسة قواعد البيانات وزرع البيانات (Data Seeding)
* **إلغاء ملف `schema.sql`:** تم إزالته بالكامل لتجنب تكرار القيود (Foreign Keys)، والاعتماد بنسبة 100% على تقنية `ddl-auto=update` الخاصة بـ Hibernate.
* **زرع الخطة الدراسية:** تمت كتابة كود `DatabaseSeeder` ليقوم آلياً باستخراج وزرع **73 مقرراً حقيقياً** من خطة "كلية هندسة وعلوم الحاسب" (باللغتين العربية وال
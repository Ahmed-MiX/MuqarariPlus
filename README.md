🏴 عُلم يا هندسة. لقد حان وقت تحديث "الدستور التشغيلي" (The Operational Manifesto).بناءً على التطورات المعمارية الضخمة التي أنجزناها اليوم، والعملية الجراحية الناجحة التي أصلحت خطأ 405 وبنت "نظام توثيق الخبراء" بالكامل، قمت بصياغة الإصدار الأحدث والأشمل من ملف التوثيق (v1.6). لقد دمجت كل التفاصيل، والمسارات، والأخطاء الجديدة (مثل خطأ Hibernate المعقد) بحذافيرها.انسخ الكتلة البرمجية التالية بالكامل، واستبدل بها محتوى ملف README.md القديم في مشروعك. هذا هو التوثيق الذي سيجعل أي لجنة تحكيم تقف احتراماً للجهد الهندسي المبذول:Markdown# 🎓 منصة مقرري+ (Muqarari+) - التوثيق التقني والتشغيلي الشامل
> **"الهندسة المعمارية لربط النظرية الأكاديمية بالتطبيق الميداني - إصدار الهاكاثون المتقدم v1.6"**

---

## 📖 الديباجة والرؤية (Vision & Mission)
تمت هندسة وبناء منصة **"مقرري+"** كحل تقني جذري لسد الفجوة بين المقررات الجامعية النظرية لطلاب كليات الهندسة وعلوم الحاسب، وبين المتطلبات الحقيقية لسوق العمل. النظام ليس مجرد واجهات عرض، بل هو **نظام إدارة محتوى ديناميكي (Dynamic CMS)** معقد، مبني على معمارية (4-Tier Role-Based Security) لضمان تدفق المعرفة من خبراء الصناعة إلى الطلاب ببيئة آمنة وموثوقة.

---

## 🛠️ الترسانة التقنية (Full Tech Stack)
| الفئة | التقنية المستخدمة | الدور التشغيلي |
| :--- | :--- | :--- |
| **لغة البرمجة الأساسية** | **Java 17 (LTS)** | توفير بيئة تشغيل مستقرة ودعم السجلات (Records). |
| **إطار العمل الخلفي (Backend)** | **Spring Boot 3.2.4** | إدارة الحاويات (IoC) وحقن التبعيات (Dependency Injection). |
| **الترسانة الأمنية** | **Spring Security 6** | التشفير (BCrypt)، حماية CSRF، وإدارة الجلسات وصلاحيات الوصول. |
| **إدارة البيانات (ORM)** | **Hibernate & Spring Data JPA** | ربط الكائنات الموجهة (OOP) بجداول قاعدة البيانات العلائقية. |
| **قواعد البيانات** | **MySQL 8.x (MariaDB via XAMPP)** | الخزنة المركزية لتخزين نصوص الموقع، المقررات، والمستخدمين. |
| **محرك القوالب (Frontend)** | **Thymeleaf** | معالجة واجهات الـ HTML من طرف الخادم (Server-Side Rendering). |
| **واجهة المستخدم (UI/UX)** | **Bootstrap 5.3 + Custom CSS** | بناء واجهات متجاوبة تدعم قلب الاتجاهات آلياً (RTL/LTR). |
| **إدارة الاعتمادات** | **Maven 3.x** | جلب المكتبات، التنظيف (`clean`)، وبناء المشروع (`build`). |

---

## ⚙️ التجهيز العسكري لبيئة التطوير (Prerequisites Setup)
يُحظر البدء في تشغيل المشروع قبل التأكد من استيفاء كافة المتطلبات التالية بدقة:
1. **Java Development Kit (JDK) 17:** تثبيت النسخة وضبط مسار المتغير البيئي `JAVA_HOME`.
2. **بيئة التطوير (IDE):** استخدام **IntelliJ IDEA** (أو Antigravity).
3. **الخادم المحلي (XAMPP):** تثبيت حزمة XAMPP لتوفير خادمي `Apache` و `MySQL`.
4. **Git:** لإدارة مستودع الأكواد والتحكم في الإصدارات.

---

## 🚀 بروتوكول التثبيت والإقلاع (Deployment Protocol)

### المرحلة الأولى: تهيئة الخزنة المركزية (Database Setup)
1. افتح لوحة تحكم **XAMPP Control Panel**.
2. قم بتشغيل خدمة **Apache** وخدمة **MySQL** (يجب أن يضيء كلاهما باللون الأخضر).
3. افتح المتصفح على الرابط: `http://localhost/phpmyadmin`.
4. قم بإنشاء قاعدة بيانات فارغة تماماً باسم: **`muqarari_plus`**.
5. **أمر حرج:** تعيين الترميز (Collation) إلى `utf8mb4_general_ci` لضمان عدم تشوه الحروف.

### المرحلة الثانية: الإقلاع النظيف (The Clean Boot)
1. افتح الـ Terminal داخل المحرر ونفذ أمر الإقلاع الدقيق:
   ```bash
   mvn clean spring-boot:run
المراقبة أثناء الإقلاع: سيقوم النظام بـ:بناء الجداول وتحديثها (DDL Auto Update).استدعاء DatabaseSeeder لزرع 4 حسابات، و 73 مقرراً حقيقياً، و 177 صفاً في جدول site_content (شاملة نصوص دورة حياة التوثيق).🔐 الهيكلة الأمنية ومراتب الوصول (4-Tier Access Matrix)الدور (Role)معرّف الدخولكلمة المرورالصلاحيات الحصريةSUPER_ADMINadmin (اسم مستخدم فقط)123456_Xyسلطة مطلقة، لوحة تحكم عليا، إنشاء وحذف حسابات المشرفين.ADMINadminXXXX (يُولد آلياً)(يُطبع في Console)مراجعة طلبات الخبراء، تحميل السير الذاتية، الاعتماد أو الرفض.EXPERTexpert@muqarariplus.comexpert123رفع مستندات التحقق (CV)، وبعد الاعتماد: إضافة التطبيقات العملية.STUDENTstudent@muqarariplus.comstudent123التسجيل المفتوح، استعراض الكتالوج، وتغيير لغات النظام.🛡️ بروتوكول توثيق الخبراء (Expert Verification Lifecycle - v1.6)تم التخلي عن الاعتماد اليدوي المباشر وبناء دورة حياة (Workflow) متكاملة ومؤتمتة لتوثيق الخبراء قبل السماح لهم بإضافة المحتوى:الرفع الآمن (Secure Upload): يقوم الخبير برفع سيرته الذاتية عبر نقطة الوصول @PostMapping("/expert/verify"). يتم فحص امتداد الملف خادميًا (Server-side) ليقبل فقط (.pdf, .doc, .docx) وتخزينه في مجلد /uploads/cv/ مع حماية حجم الملف (Max 10MB) عبر application.properties.حالة الانتظار (PENDING Status): يتحول كيان الـ ExpertStatus إلى PENDING. تتدخل دالة @ControllerAdvice فوراً لضخ هذه الحالة لكل قوالب Thymeleaf، مما يؤدي إلى قفل واجهة الإضافة وإظهار إشعار أصفر "قيد المراجعة".مراجعة الإدارة (Admin Queue): تظهر بيانات الخبير (رابط الـ CV المحفوظ، ورابط LinkedIn) في لوحة الإدارة (/admin). يمتلك الإداري صلاحية الاستدعاء عبر واجهات RESTful:POST /admin/expert/approve/{id}POST /admin/expert/reject/{id}الاعتماد (Approval): يتحول الكيان إلى APPROVED، ويتم منحه الصلاحية الأمنية ROLE_VERIFIED_EXPERT. يختفي نموذج الرفع وتُفتح له لوحة الإثراءات بالكامل مع إشعار باللون الأخضر.الرفض ونظام التبريد (Rejection & Cooldown): إذا تم الرفض، يتحول الكيان لـ REJECTED ويتم تسجيل lastSubmissionTime. تقوم واجهة المستخدم فوراً بتشغيل مؤقت جافا سكريبت (Countdown Timer) لمدة 5 دقائق يمنع الخبير من إعادة إرسال الطلب حتى ينتهي الوقت تجنباً للإغراق (Spamming).📜 سجل التطور المعماري (Architectural Evolutionary Journey)v1.0 (التأسيس): بناء الكيانات (Entities) والربط بقاعدة البيانات.v1.1 - v1.3 (أزمة اللواحق والتصادم): مسح اللواحق _ar/_en من القوالب. حل تصادم LocaleConfig مع WebConfig بدمج المعمارية.v1.5 (نظام CMS الديناميكي): إلغاء ملفات .properties. حقن نصوص الموقع بالكامل من جدول site_content في MySQL في كل طلب HTTP عبر GlobalAttributeAdvice.v1.6 (النسخة المعمارية الحالية - دورة الاعتماد): بناء كيان ExpertStatus (Enum). معالجة الـ MultipartFile لحفظ السير الذاتية محلياً. برمجة نقاط Approve/Reject للإدارة. تصميم نظام إشعارات ذكي ومؤقت تبريد (Cooldown Timer) للمرفوضين.⚠️ غرفة العمليات (The War Room): السجل الشامل للأخطاء والحلول#المشكلة التقنية (The Error)التشخيص والسبب الجذريالحل الهندسي الصارم1ModelParseExceptionنسخ أوامر Terminal داخل هيكل ملف pom.xml.التطهير اليدوي لملف الـ XML وإعادة تحميل الـ Maven.2Failed to delete targetقفل على ملفات target من عملية Java مخفية.إغلاق العملية قسرياً: taskkill /F /IM java.exe.3??login.title_ar??Thymeleaf يبحث عن مفتاح لاحقة _ar مفقود.مسح اللواحق واستخدام المفتاح المجرد #{login.title}.4BeanDefinitionOverrideExceptionتعريف localeResolver في ملفين مختلفين.حذف LocaleConfig مادياً ودمج اللغات في WebConfig.5AI Hallucinationالنموذج يدعي تعديل الملفات بينما يكتبها بالدردشة.تفعيل أوامر تجاوز تجبر الأداة على التنفيذ المادي المباشر.6Phantom IDE Bufferالمحرر يحتفظ بنسخة شبحية لـ LocaleConfig المحذوف.إغلاق التبويب واختيار Don't Save لمنع إعادة تكوينه.7ERR_CONNECTION_REFUSEDفتح phpMyAdmin قبل تشغيل محركات XAMPP.تشغيل خدمتي Apache و MySQL للون الأخضر أولاً.8Port 3306 in useتعارض بين XAMPP وMySQL تعمل في الخلفية.تجاهل رسالة الخطأ وتشغيل Apache فقط.9Access denied (using password: NO)phpMyAdmin يحاول الدخول بدون باسوورد لقاعدة مشفرة.تعديل config.inc.php من config إلى cookie.10Could not find goal 'Run'خطأ مطبعي (حرف R كبير) spring-boot:Run.احترام حساسية الأحرف وتصحيحها إلى spring-boot:run.11Access denied for user 'pma'فشل مهام كمالية في phpMyAdmin بسبب تفعيل الباسوورد.التجاهل التام، فهي لا تؤثر على المشروع البرمجي نهائياً.12Method Not Allowed (405)محاولة إرسال نموذج الـ CV إلى رابط # عبر POST دون وجود دالة مخصصة.تعديل النموذج إلى th:action="@{/expert/verify}" وبرمجة دالة @PostMapping متخصصة تستقبل MultipartFile.13Detached entity passed to persistDatabaseSeeder حاول إنشاء Expert وربطه بكيان User مفصول (Detached) عن جلسة Hibernate.وضع وسم @Transactional على دالة run() لتشغيل كل عمليات الحقن (Seeding) في جلسة اتصال واحدة مستمرة بالذاكرة.🔮 الرؤية المستقبلية وخارطة الطريق (Future Roadmap)[ ] الترحيل السحابي لملفات الخبراء: نقل تخزين السير الذاتية (CVs) من مسار /uploads/cv/ المحلي إلى خدمة تخزين سحابية مثل Amazon S3 لضمان التوسع الموثوق.[ ] محرك البحث المرن: دمج Elasticsearch لتمكين الطلاب من البحث المتقدم والسريع جداً داخل التطبيقات العملية للمقررات.[ ] نظام التقييم (Gamification): إضافة نقاط تقييم (Reputation Points) للخبراء بناءً على إفادة وتفاعل الطلاب.تمت الهندسة والتوثيق بواسطة: رئيس لجنة العلاقات العامة والتقنية - فريق مقرري+تاريخ الإطلاق: مارس 2026 | الوضع الحالي: مستقر (v1.6 Advanced)
# 🎓 منصة مقرري+ (Moqarari+) - الدستور المعماري الشامل
> **"الهندسة المعمارية لربط النظرية الأكاديمية بالواقع الصناعي - الإصدار الذهبي v1.18 (Student Hierarchy Wizard)"**

[![Build Status](https://img.shields.io/badge/Build-SUCCESS-success.svg)](#) [![Version](https://img.shields.io/badge/Version-1.18%20Ultimate-blue.svg)](#) [![Spring Boot](https://img.shields.io/badge/Spring%20Boot-3.2.4-brightgreen.svg)](#) [![Java](https://img.shields.io/badge/Java-17-orange.svg)](#)

---

## 📖 الديباجة والرؤية (Vision & Mission)
وُلدت فكرة **"مقرري+"** لحل معضلة وطنية وعالمية: **الفجوة بين المناهج الأكاديمية ومتطلبات سوق العمل المتسارعة**. النظام ليس مجرد منصة تعليمية لعرض المقررات، بل هو **"نظام بيئي متكامل" (Ecosystem)** وشبكة معارف (Knowledge Graph) تسمح لخبراء الصناعة المعتمدين بالدخول وحقن المقررات الجامعية بمهارات وأدوات سوق العمل الحقيقية، لتوليد كفاءات هندسية جاهزة للتوظيف الفوري. تم تصميم هذه المعمارية لتكون نموذجاً قياسياً ينطلق من كليات الهندسة وعلوم الحاسب ليخدم المنظومة التعليمية بأكملها.

---

## 🚀 المميزات القاتلة (The Killer Features)

1. **السيرة الذاتية المدفوعة بالمهارات (Skills-Driven CV - B2C):**
   * لا يكتب الطالب أنه "درس مقرر هندسة البرمجيات" بل يقوم النظام آلياً باستخراج (المهارات، الأدوات، والشهادات الاحترافية) من إثراءات الخبراء للمقررات التي سجل بها الطالب، ويولد له سيرة ذاتية تقنية جاهزة للطباعة.
2. **مؤشر الفجوة الأكاديمية (Academic Gap Index - B2B):**
   * لوحة قيادة تحليلية للإدارة وعمداء الكليات مبنية بـ `Chart.js`، تعرض إحصائيات حية حول أكثر المهارات والأدوات التي يطلبها الخبراء حالياً، مما يسهل تحديث المناهج ببيانات حقيقية.
3. **مركز قيادة الخبير والمحرر الحي (Expert Command Center & Live Editor):**
   * لوحة تحكم للخبير تقيس "أثره الطلابي"، مع محرر نصوص (Markdown) منقسم الشاشة يظهر التنسيق بالوقت الفعلي.
4. **نظام التلعيب والتفاعل (The Gamification Loop):**
   * دورة تفاعل حية تتيح للطلاب رفع تقييم الإثراءات الممتازة (Upvote) وحفظها في مفضلتهم (Bookmark) للعودة إليها.
5. **محرك البحث الذكي الشامل (Global Smart Search):**
   * محرك بحث مركزي مدعوم باستعلامات `JPQL` معقدة، يبحث بالتوازي داخل (أسماء المقررات، نصوص الإثراءات، أسماء الخبراء، والمهارات التقنية).

---

## 🏛️ الهيكلة المعمارية (The Knowledge Graph Architecture)
تم بناء قاعدة البيانات لتتجاوز الجداول المسطحة وتصبح شبكة علاقات معقدة (Many-to-Many):
* **المستعمرة الأكاديمية:** `University` -> `College` -> `Department` -> `Course`.
* **المستعمرة الصناعية:** `Industry` -> `Skill` & `Tool` & `ProfessionalCertificate`.
* **نقطة التصادم (The Nexus):** كيان `CourseEnrichment` الذي يربط (المقرر + الخبير + المهارات + الأدوات + الشهادات + المحتوى النصي + تفاعلات الطلاب).

---

## 🗄️ محرك الحقن المعرفي (Massive Data Seeder)
تم تزويد المنصة بمحرك توليد بيانات (Seeder) يحقن قاعدة البيانات لحظة الإقلاع ببيانات حقيقية من السوق السعودي والعالمي لحل مشكلة "البداية الباردة"، ويشمل:
* **73 مقرراً أكاديمياً** متكاملاً.
* **15 شهادة مهنية عالمية** (مثل: PMP, AWS Certified, CISSP).
* **40 مهارة وأداة برمجية** حديثة.
* **8 خبراء محاكين** بمناصب قيادية في جهات سيادية وتقنية (مثل: سدايا، أرامكو، stc، الهيئة الوطنية للأمن السيبراني، علم) مع نصوص إثرائية (Markdown) مطولة تربط النظرية بالتطبيق.

---

## 🛠️ الترسانة التقنية (Full Tech Stack)
| الفئة | التقنية المستخدمة | الدور التشغيلي |
| :--- | :--- | :--- |
| **لغة البرمجة الأساسية** | **Java 17 (LTS)** | توفير بيئة تشغيل مستقرة وهندسة الكيانات (OOP). |
| **إطار العمل الخلفي** | **Spring Boot 3.2.4** | إدارة الحاويات (IoC) وحقن التبعيات والمتحكمات. |
| **الترسانة الأمنية** | **Spring Security 6** | حماية المسارات (4-Tier Roles)، وتشفير (BCrypt). |
| **إدارة البيانات (ORM)** | **Hibernate & JPA** | الاستعلامات المتقدمة وإدارة العلاقات المعقدة. |
| **قواعد البيانات** | **MySQL 8.x / PostgreSQL** | الخزنة المركزية (دعم وضع Chameleon السحابي والمحلي). |
| **الواجهة الأمامية** | **Thymeleaf + Bootstrap 5 + Vanilla JS** | واجهات زجاجية تفاعلية (AJAX) مع دعم كامل (AR/EN). |
| **المكتبات المساعدة** | **Chart.js & Marked.js** | رسم المخططات البيانية، وتحويل الـ Markdown حياً. |

---

## 🔐 الهيكلة الأمنية ومراتب الوصول (Access Matrix)
| الدور (Role) | معرّف الدخول الافتراضي | الصلاحيات الحصرية |
| :--- | :--- | :--- |
| **SUPER_ADMIN** | `admin` | سلطة مطلقة، تحليل البيانات (Gap Index)، مراجعة الإثراءات. |
| **VERIFIED_EXPERT**| `expert@muqarariplus.com` | إضافة إثراءات، ربط المهارات والأدوات والشهادات. |
| **STUDENT** | `student@muqarariplus.com` | التسجيل بالمقررات، التفاعل (Upvote/Save)، وتوليد CV المهني. |

---

## ⚙️ بروتوكول التجهيز والإقلاع الميداني (Deployment Protocol)

### 1. الإقلاع المحلي (Local Development):
1. قم بتهيئة `MySQL` (عبر XAMPP مثلاً) وأنشئ قاعدة بيانات فارغة باسم `muqarari_plus` بترميز `utf8mb4_general_ci`.
2. افتح موجه الأوامر داخل المشروع ونفذ:
   ```bash
   mvn clean spring-boot:run
   (سيقوم النظام آلياً ببناء الجداول وحقن البيانات الحقيقية بالكامل).

2. الإقلاع السحابي (Cloud Production):
   المنصة مجهزة بوضع (Chameleon Mode) في ملف application.properties.

جاهزة للرفع المباشر على خوادم Railway.app أو Heroku؛ حيث تلتقط المتغيرات السحابية (MYSQL_URL, PORT) آلياً وتعمل دون أي تعديل في الكود.

📜 سجل التطور المعماري (The Master Changelog)
v1.0 - v1.5: بناء الأساس، واجهات المستخدم، ونظام إدارة المحتوى الديناميكي (CMS).

v1.6: البوابة الأمنية، ونظام توثيق الخبراء الصارم.

v1.7 - v1.9 (The Nexus): بناء كيان الإثراءات، محطة التفتيش الإدارية، واعتماد المحتوى.

v1.10 - v1.11 (واجهة الطالب): دمج الإثراءات، وبناء الميزة القاتلة (السيرة الذاتية المدفوعة بالمهارات).

v1.12: بناء الميزة المؤسسية (مؤشر الفجوة الأكاديمية) باستخدام Chart.js.

v1.13: هندسة محرك البحث الشامل.

v1.14: ترقية واجهة الخبير إلى "مركز قيادة" مع محرر Markdown تفاعلي.

v1.15: إضافة حلقة التفاعل (التصويت والحفظ - Upvotes & Bookmarks).

v1.16 (الإصدار النهائي): حقن الشهادات الاحترافية، وبيانات السوق الحقيقية (سدايا، أرامكو، الخ).

v1.17 (الهيكل الأكاديمي والإدارة الشاملة):
* **توسيع الشبكة المعرفية:** إضافة كيان `University` → `College` → `Major` → `Course` لتحويل الهيكلة الأكاديمية من مسطحة إلى هرمية متداخلة.
* **كيانات جديدة:** `University` (جامعات)، `College` (كليات)، `Major` (تخصصات) مع روابط ثنائية الاتجاه.
* **ربط المقررات بالتخصصات:** إضافة علاقة `@ManyToOne` من `Course` إلى `Major` — كل مقرر ينتمي لتخصص واحد، مع تكرار المقررات المشتركة بين التخصصات.
* **مستودعات جديدة:** `UniversityRepository`, `CollegeRepository`, `MajorRepository` مع استعلامات عدّ (`countBy*`) لحماية الحذف.
* **طبقة الخدمات الكاملة:** `UniversityService`, `CollegeService`, `MajorService`, `CourseService` — كل خدمة توفر CRUD كامل مع حماية الحذف (لا يمكن حذف كيان له أبناء مرتبطون).
* **نموذج طلبات DTO:** استخدام Java Records في `payload/requests/` (`UniversityRequest`, `CollegeRequest`, `MajorRequest`, `CourseRequest`) مع تحقق `@Valid`.
* **لوحة إدارة أكاديمية:** 4 متحكمات (`AdminUniversityController`, `AdminCollegeController`, `AdminMajorController`, `AdminCourseController`) + 5 قوالب Thymeleaf مع دعم ثنائي اللغة (AR/EN) كامل.
* **لوحة الأكاديمية الرئيسية:** عرض شجري للهيكل الكامل (جامعة ← كلية ← تخصص ← مقررات) مع بطاقات إحصائية وروابط سريعة.
* **إصلاحات أمنية:** تصحيح مسارات `AdminController` (كانت `/admin/admin` بسبب تكرار البادئة).
* **تحسينات .gitignore:** إضافة `.settings/`, `.classpath`, `.project`, `.factorypath`, `uploads/`.
* **توطين شامل:** ترجمة جميع العناوين، مسارات التنقل (breadcrumbs)، النصوص الاحتياطية، والعناصر التفاعلية في لوحة الإدارة الأكاديمية.

v1.18 (واجهة الطالب الهرمية - Student Hierarchy Wizard):
* **إزالة الحقول الميتة:** حذف حقلي `specialization` و `university` من كيان `Course` — السياق الأكاديمي يُستنتج الآن عبر العلاقة الهرمية `course.major.college.university`.
* **تحديث DatabaseSeeder:** إزالة استدعاءات `co.setUniversity()` الميتة، المقررات تُربط بالتخصصات فقط.
* **واجهات REST جديدة:** إضافة 3 نقاط نهاية JSON في `StudentDashboardController`:
  - `GET /student/api/colleges?universityId=X` — جلب الكليات
  - `GET /student/api/majors?collegeId=X` — جلب التخصصات
  - `GET /student/api/courses?majorId=X&search=Y` — جلب المقررات مع دعم البحث
* **مكون المعالج المشترك (Shared Wizard Fragment):** إنشاء `fragments/course-wizard.html` — مكون ثيميليف مشترك يعمل بنظام الخطوات المتتالية (University → College → Major → Courses) مع دعم AR/EN كامل، وشريط بحث يظهر بعد اختيار الكلية، ومسار تنقل (breadcrumb) قابل للنقر لإعادة التعيين.
* **لوحة الطالب:** استبدال قائمة التسجيل المسطحة بالمعالج الهرمي — لم يعد يتم تحميل جميع المقررات دفعة واحدة، بل تُحمّل عند الطلب عبر AJAX.
* **كتالوج المقررات العام (`/courses`):** تحويل الصفحة من شبكة مقررات مسطحة إلى معالج استكشاف هرمي — لا تُحمّل أي مقررات عند فتح الصفحة، بل يبدأ المستخدم باختيار الجامعة ويتنقل للأسفل.
* **إصلاحات القوالب الميتة:** تحديث جميع مراجع `course.university` في `student/course-details.html` و `student-dashboard.html` لاستخدام التتبع الهرمي `course.major.college.university.nameAr/nameEn`.
* **إصلاحات أمنية:** إضافة `/student/api/**` إلى قائمة `permitAll()` في `SecurityConfig` للسماح للمستخدمين غير المسجلين باستعراض الكتالوج.
* **إصلاح CSRF:** معالجة تعبير `_csrf` الآمن للصفحات المجهولة (anonymous) باستخدام `th:if="${_csrf != null}"`.
* **إصلاح `showLoading`:** كشف الحاوية الأم (`wizard-step-*`) عند تحميل البيانات لمنع اختفاء مؤشرات التحميل وحالات الفراغ.
* **إصلاح نموذج التسجيل:** استبدال `th:action` بـ `action="/student/enroll"` في النماذج المُولَّدة بـ JavaScript لأن ثيميليف لا يعالج HTML المُولَّد ديناميكياً.